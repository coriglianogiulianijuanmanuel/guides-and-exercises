const browserType = "mozilla";
console.log(browserType.length); //this returns the string length
const myString = "this is a string"; //spaces also count!
console.log(myString.length);

console.log(browserType[0]); //this returns the character in [] (the first character is 0)
console.log(myString[4]);

console.log(browserType[browserType.length - 1]); //this returns the last character
console.log(myString[myString.length - 1]);
