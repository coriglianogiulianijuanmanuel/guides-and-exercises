const myAge = 24;
let nextAge;

if (myAge === 24) {
  nextAge = myAge;
  nextAge++;
  console.log(`I'm actually ${myAge}, and the next year will be ${nextAge}`);
} else {
  console.log("I'm not 24... :c");
}
